import {StyleSheet} from 'react-native';

export const styled = StyleSheet.create({
    container: {
        padding: 18,
        height: '100%',
        alignItems: 'center',
        backgroundColor: '#353535',
    },
});
