import {StyleSheet} from 'react-native';

export const styled = StyleSheet.create({
    container: {
        paddingTop: 80,
    },
    text: {
        fontSize: 18,
        color: '#fff',
    },
});
